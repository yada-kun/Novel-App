import Layout from "@/layout";

const Signin = () => {
    return (
        <div>
            <Layout>
                <h1>Helloooon</h1>
            </Layout>
        </div>
    );
};

export default Signin;
